{
    "name": "arise-hrm-root",
    "private": true,
    "scripts": {
        "setup": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
        "frontend": "cd frontend && npm run dev",
        "backend": "cd backend && npm run dev",
        "dev": "echo \"Please run 'npm run backend' and 'npm run frontend' in separate terminals.\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    },
    "dependencies": {
        "axios": "^1.13.2"
    }
}
